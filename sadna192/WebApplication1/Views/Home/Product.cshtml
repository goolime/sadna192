@using sadna192
@{
    ViewData["Title"] = "Product";
}

<h2>@ViewData["store"] : @ViewData["product"]</h2>
<!--->if (((I_User_ServiceLayer)ViewData["SL"]).isOwner((string)ViewData["store"]))
{
    <button>Edit</button>
}-->
<button>Buy</button>